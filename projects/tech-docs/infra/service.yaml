apiVersion: v1
kind: Service
metadata:
  name: tech-docs
  namespace: tech-docs
  labels:
    app: tech-docs
spec:
  type: ClusterIP
  ports:
  - name: http
    port: 8000
    protocol: TCP
    targetPort: 8000
  selector:
    app: tech-docs

